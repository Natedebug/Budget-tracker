# Database Configuration
# PostgreSQL connection string (Neon recommended for serverless)
DATABASE_URL=postgresql://user:password@host:5432/database
PGHOST=your-db-host.neon.tech
PGPORT=5432
PGUSER=your-username
PGPASSWORD=your-password
PGDATABASE=budgetsync

# Session Management
# Generate a random string for session encryption
# Example: openssl rand -base64 32
SESSION_SECRET=your-random-session-secret-here

# OpenAI API (for AI-powered receipt scanning)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key

# Replit Auth (OIDC Authentication)
# These are automatically provided when running on Replit
# Only needed for local development if you want to test auth
# REPLIT_AUTH_CLIENT_ID=your-client-id
# REPLIT_AUTH_CLIENT_SECRET=your-client-secret
# REPLIT_AUTH_ISSUER_URL=https://replit.com/oidc

# Gmail API (optional - for automatic receipt email import)
# Configured via Replit Gmail connector
# No manual setup required when using Replit
# GMAIL_CLIENT_ID=
# GMAIL_CLIENT_SECRET=
# GMAIL_REDIRECT_URI=

# Application Settings
NODE_ENV=development
PORT=5000
